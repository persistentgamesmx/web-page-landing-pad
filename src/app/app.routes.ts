import { Routes } from '@angular/router';
import { LandingpadComponent } from './landingpad/landingpad.component';

export const routes: Routes = [
  {
    path: '',
    component: LandingpadComponent
  }
];
